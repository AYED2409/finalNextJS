import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'About',
}

export default function Page() {
    return (
        <div>
            <h1>Pagina about</h1>
        </div>
    );
}